echo "🚀 Running pre-push checks..."

# Only run E2E tests on push to main
current_branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$current_branch" = "main" ]; then
  echo "1️⃣  Running server E2E tests..."
  npm run test:e2e || exit 1
  
  echo "2️⃣  Running Docker E2E tests..."
  npm run docker:test || exit 1
  
  echo "ℹ️  Skipping tool E2E tests (require actual Actual Budget database)"
  echo "   Tool E2E tests will run in CI with mock data"
fi

echo "✅ All pre-push checks passed!"
